on:
  push:
    branches: main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Test
      run: | 
        echo ${{github.event.commit.modified}};
        echo ${{contains(github.event.commit.modified, "Interfaces/cpp/setup-gitpod.sh")}};
